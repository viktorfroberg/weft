-- Initial schema placeholder.
-- Phase 2 will populate: projects, workspaces, workspace_repos, tasks,
-- task_worktrees, workspace_sections, settings.

CREATE TABLE IF NOT EXISTS _placeholder (
    id INTEGER PRIMARY KEY
);
